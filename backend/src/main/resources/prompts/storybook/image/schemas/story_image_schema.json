{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StableDiffusionImageResponse",
  "description": "A schema for the response from the AUTOMATIC1111 Stable Diffusion API.",
  "type": "object",
  "properties": {
    "images": {
      "type": "array",
      "description": "A list of Base64-encoded image strings.",
      "minItems": 1,
      "items": {
        "type": "string",
        "contentEncoding": "base64",
        "description": "A single image encoded as a Base64 string."
      }
    },
    "parameters": {
      "type": "object",
      "description": "The parameters used for the generation."
    },
    "info": {
      "type": "string",
      "description": "Information about the generation process."
    }
  },
  "required": ["images"]
}
