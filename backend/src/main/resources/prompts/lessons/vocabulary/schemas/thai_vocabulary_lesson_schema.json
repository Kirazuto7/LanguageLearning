{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ThaiVocabularyLesson",
  "description": "A schema for a Thai vocabulary lesson component.",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the vocabulary lesson in English."
    },
    "vocabularies": {
      "type": "array",
      "description": "A list of vocabulary words related to the topic.",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "object",
        "properties": {
          "englishTranslation": {
            "type": "string",
            "description": "The English translation of the word.",
            "pattern": "^[A-Za-z0-9'() -]+$"
          },
          "thaiScript": {
            "type": "string",
            "description": "The word in Thai script.",
            "pattern": "^[\\u0E00-\\u0E7F \\u200B]+$"
          },
          "romanization": {
            "type": ["string", "null"],
            "description": "The romanized (RTGS) spelling of the word.",
            "pattern": "^[a-zA-Z\\u00C0-\\u00FF\\u0100-\\u017F- ]*$"
          },
          "tonePattern": {
            "type": ["string", "null"],
            "description": "The tone pattern of the word (e.g., 'mid', 'low', 'falling', 'high', 'rising').",
            "pattern": "^(mid|low|falling|high|rising)$"
          }
        },
        "required": ["englishTranslation", "thaiScript"]
      }
    }
  },
  "required": ["title", "vocabularies"]
}